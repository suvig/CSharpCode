# WalmartHW-XMLMerge code shell

Run this from the `WalmartHW-XMLMerge/` folder:

```bash
dotnet run
```

Paste your code into `WalmartHW-XMLMerge/UserCode.cs` (`UserCode.Run`).

Output is written to `WalmartHW-XMLMerge/Output.xml`.
